# another thing how it would work is like this

const { token } = require('./config.json');

just make a new file in the dictionary where ur script is located and make config.json then paste this

{
  "token": "{insert token}",
}
